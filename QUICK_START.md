# Quick Start: Enable GitHub Actions Deployment

## 🚀 3 Simple Steps

### 1. Commit and Push
```bash
git add .
git commit -m "Add GitHub Actions deployment"
git push origin master
```

### 2. Switch to GitHub Actions
- Go to: **Repository** → **Settings** → **Pages**
- Change **Source** from "Branch: master" to **"GitHub Actions"**
- Click **Save**

### 3. Trigger Deployment
- Go to: **Actions** tab
- Click **"Run workflow"** on "Deploy to GitHub Pages"
- Select `master` branch
- Click **"Run workflow"**

## ✅ That's It!

Your site will be deployed automatically. Check the **Actions** tab to see progress.

Your site will be live at: **http://madugufoundation.com/**

---

**Need help?** See `SWITCH_TO_GITHUB_ACTIONS.md` for detailed instructions.

