source 'https://rubygems.org'

ruby '>= 2.7.0'

# Template engine (ERB is built-in, but we'll use it)
# File utilities
gem 'fileutils', '~> 1.0'

# Optional: For better file handling
gem 'pathname', '~> 0.1'

# Development dependencies
group :development do
  gem 'rake', '~> 13.0'
end

